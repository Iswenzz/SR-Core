#include "commonsetup.template"
techniqueSet("sr_spray");

textureTable
{
    #if "$colorMap$" == ""
        #error "colorMap may not be blank in 2d materials"
    #endif
    "colorMap" = map("@tileColor@", "@filterColor@", "$colorMap$", @nopicmipColor@) "@formatColor@" : "2d";
}

constantTable
{
	"atlasSize" = float2(@textureAtlasRowCount@, @textureAtlasColumnCount@);
}

refImage("$colorMap$");
